part of 'view.dart';

/// View of the color changer feature
class ColorChangerView extends StatelessWidget {
  /// Constructor to create the view
  const ColorChangerView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
